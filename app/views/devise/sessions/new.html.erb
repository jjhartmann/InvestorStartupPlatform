<header class='back-image'>

</header>
<%= content_for :body_class, 'index' %>
<%= content_for :body_id, 'page-top' %>

<%= react_component('Login', props: {data: {authenticity_token: form_authenticity_token}}) %>
