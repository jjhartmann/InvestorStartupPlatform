<%= content_for :body_class, "dashbord" %>
<%= content_for :sidebar_dashboard_class, "active" %>

<%= render "/user_dashboards/profile" %>
<div class="main-body">
  <%= render "/user_dashboards/sidebar" %>
  <div class="container side-deatail">
    <div class="col-xs-12">
      <div class="profile-tabs">
        <div class="tab-content">
          <div class="tab-pane fade in active">
            <div class="tab-title">New Proposal</div>
            <ul class="main-dashbord">
              <%= render 'form', proposal: @proposal %>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
